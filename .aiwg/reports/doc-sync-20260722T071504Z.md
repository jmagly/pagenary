# Documentation–Code Sync Report

- Date: 2026-07-22T07:15:04Z
- Direction: code-to-docs
- Mode: incremental (current Git changes)
- Scope: accessible image viewport and shared Mermaid/image pan-zoom runtime

## Scope audited

Implementation and validation sources:

- `apps/publisher/src/lib/pan-zoom.js`
- `apps/publisher/src/mermaid-init.js`
- `apps/publisher/src/app.js`
- `apps/publisher/src/styles.css`
- `apps/publisher/scripts/build-tenants.js`
- `apps/publisher/scripts/lib/accessibility-linter.js`
- related media/accessibility tests

Documentation surfaces:

- Publisher README and developer/API/architecture documentation
- tenant image and Mermaid authoring reference
- accessible-authoring and page-effects guidance
- showcase documentation and example content

## Drift findings and fixes

1. **API cleanup contract:** `renderMermaidBlocks` now resolves to a teardown
   callback rather than `void`. Updated the API reference and example.
2. **Shared runtime:** the new `lib/pan-zoom.js` public functions and lifecycle
   responsibilities were absent from API and architecture docs. Added them.
3. **Mermaid architecture:** the architecture example referenced Mermaid 11 and
   omitted shared viewport enhancement. Aligned it with the implemented Mermaid
   10 import and controller delegation.
4. **Feature discovery:** README and developer guidance mentioned Mermaid but
   not opt-in interactive SVG/PNG/JPEG images. Added concise discovery text and
   both authoring forms.
5. **Interaction contract:** Mermaid docs described mouse drag only. Documented
   keyboard, pointer/pen, touch/pinch, shared scale bounds, and reset behavior.
6. **Zoom choice:** showcase docs did not distinguish modal `data-pe-zoom` from
   the bounded pan-and-zoom viewport. Added selection guidance and an example.

## Files changed by this sync

- `apps/publisher/README.md`
- `apps/publisher/docs/API.md`
- `apps/publisher/docs/ARCHITECTURE.md`
- `apps/publisher/docs/DEVELOPER-GUIDE.md`
- `apps/publisher/docs/SHOWCASE.md`
- `apps/publisher/docs/TENANT-CONFIG.md`

The feature's already-authored `ACCESSIBLE-AUTHORING.md`, `PAGE-EFFECTS.md`, and
showcase content were audited and retained.

## Validation

- `git diff --check`
- stale-contract search for `renderMermaidBlocks(...): Promise<void>`
- stale-version search for `mermaid@11`
- stale mouse-only Mermaid interaction search
- publisher documentation link/build checks through `npm run check`

## Human review

None required for the bounded feature scope. Release notes or blog coverage can
be added during a release workflow but are not required to make the current
technical and authoring documentation accurate.

## Recommended handoff

Use the repository's normal commit/review workflow when the implementation and
documentation changes are ready for delivery.
