# Hybrid React Example

This is an ordinary Pagenary documentation route. It builds through the normal
static pipeline, appears in search, contributes to Docs Map, and gets a static
snapshot.

Open [Diagnostics](diagnostics.md) to see the React app route. The route still
has authored fallback content for static hosts, crawlers, LLM consumers, and
users without JavaScript.
